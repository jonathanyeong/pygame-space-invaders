# Space Invaders 

> PyGame clone of Space Invaders

# Usage
To run this game:

1. `$ cd pyvader/`
2. `$ python launcher.py`

To test the app:

3. Run `$ nosetests` from the root project directory.

# Tracking 
